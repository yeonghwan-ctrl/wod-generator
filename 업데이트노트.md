# 업데이트노트

이 파일은 진행한 작업을 시간순으로 기록합니다. 최신 항목이 위로 옵니다.
각 항목은 하나의 커밋(복원 지점)에 대응합니다.

---

## 2026-06-04 — 색감 가독성 개선 + 하단 네비 레이아웃 + 구글 로그인만 유지
- **색감**: 너무 검던 배경(#0b0c0f)을 **네이비-차콜(#161a22)** 로 올리고
  surface/border/텍스트 대비를 높여 가독성 개선 (muted 색도 밝게)
- **레이아웃**: 상단 탭 → **하단 고정 네비게이션 바**(아이콘+라벨, 앱 느낌)로 전환.
  헤더는 브랜드+로그인/유저만 남김
- **로그인 간소화**: 애플·카카오·이메일 로그인 전부 제거, **구글 로그인만** 유지
  - `useAuth` 구글 전용으로 축소, `LoginView` 구글 버튼만
  - Kakao SDK 스크립트·`functions/`(카카오 Cloud Function)·카카오 env 제거
- 변경 파일: `src/styles.css`, `src/App.jsx`, `src/components/LoginView.jsx`,
  `src/store/useAuth.js`, `index.html`, `public/manifest.json`, `.env.example`
  (삭제: `functions/`)
- 빌드 통과(번들 축소), GitHub `main` 푸시

## 2026-06-04 — 다크/남성적 무드로 전체 디자인 리디자인
- 라이트 블루 테마 → **다크 차콜/블랙 + 스틸 블루(#5ea2ff) 강조**로 전환
  (B&W 크로스핏 짐 사진 레퍼런스 기반의 거칠고 남성적인 무드)
- 볼드·대문자·자간 강조 타이포(브랜드/탭/데이 라벨/섹션 타이틀), 더 각진 형태(radius↓)
- 홈 히어로를 밝은 블루 블록 → **다크 그라데이션 + 블루 글로우/액센트 바**로 변경
- 워밍업 패널·카드·로그인 화면·스플래시를 다크 팔레트에 맞게 재조정
- 로고(`barbell.svg`)를 다크/오렌지 → **흰색 바벨 마크**(스틸 블루 타일 위)로 정리
- `theme-color`/매니페스트 색을 `#0b0c0f`(다크)로 변경
- 변경 파일: `src/styles.css`, `index.html`, `public/manifest.json`, `public/barbell.svg`
- 빌드 통과, GitHub `main` 푸시

## 2026-06-04 — 브랜드 변경(BoxLift → W2P) + 자동 구성 리브랜딩
- 앱 이름을 **BoxLift → W2P (Work to PR)** 로 변경
  (헤더, 로그인 화면, `index.html` title, `public/manifest.json`)
- 입력 화면의 "HWPO 스타일 자동 구성" → **"W2P 자동 구성"** 으로 변경
  (부제: "올림픽·풀·스쿼트·보조를 한 세션에 균형 있게")
- 코드/주석 전반의 HWPO 표현을 W2P 자체 브랜드 표현으로 교체
  (`templates.js`, `warmups.js`, `generateProgram.js`, `useAthlete.js`, `HomeView.jsx`)
- manifest 테마색도 `#98cfff` 로 통일
- 변경 파일: `index.html`, `public/manifest.json`, `src/App.jsx`,
  `src/components/{LoginView,HomeView,OneRMForm}.jsx`,
  `src/data/{templates,warmups}.js`, `src/logic/generateProgram.js`,
  `src/store/useAthlete.js`, `functions/*`
- 빌드 통과, GitHub `main` 푸시

## 2026-06-04 — 배포 시 빈 화면(흰 화면) 버그 수정
- 원인: Firebase 환경변수가 없는 배포 환경(Vercel)에서 `getAuth()`가
  `auth/invalid-api-key` 에러를 던져 React 앱 전체가 마운트되지 못함
- 수정: Firebase 설정값이 있을 때만 초기화하도록 방어 처리(`src/firebase.js`).
  설정이 없으면 `auth = null`, 로그인 기능만 비활성화되고 앱은 정상 동작
- `useAuth`가 `auth === null`을 안전하게 처리(로그인 시도 시 안내 메시지)
- 변경 파일: `src/firebase.js`, `src/store/useAuth.js`
- 빌드 통과, GitHub `main` 푸시
- 참고: 실제 로그인을 쓰려면 Vercel 프로젝트 설정 → Environment Variables 에
  `VITE_FIREBASE_*` 키 등록 후 재배포 필요

## 2026-06-04 — 로그인 선택화(필수 → 건너뛰기 가능)
- 인증 게이트 제거: 로그인 없이도 앱 전체 사용 가능
- 헤더에 로그인 상태별 UI: 비로그인 시 **로그인** 버튼, 로그인 시 아바타+로그아웃
- 로그인 화면에 **건너뛰기** 버튼 추가, 로그인 성공 시 자동으로 홈 복귀
- 로그인 안내 문구를 "선택 사항"으로 변경
- WORKLOG.md 추가 (앞으로 작업 내역 기록)
- 변경 파일: `src/App.jsx`, `src/components/LoginView.jsx`, `src/styles.css`
- 빌드 통과, GitHub `main` 자동 푸시

## 2026-06-04 — 워밍업 + 디자인 + 로그인/홈 (초기 기능 추가)
- **워밍업 루틴**: 세션별 HWPO 스타일 4단계(체온/가동성/활성화/바벨 램프업)를
  프로그램 데이 카드에 접이식 패널로 추가. 커스텀 모드는 카테고리 기반 제네릭 워밍업.
  - 신규 `src/data/warmups.js`, 수정 `src/logic/generateProgram.js`, `src/components/DayCard.jsx`
- **디자인 개편**: 메인 컬러 `#98cfff` 팔레트, 그라데이션 헤더/버튼, 데이 카드 액센트·hover,
  fade-in 애니메이션, 주차 탭 강조. (`src/styles.css`, `index.html` theme-color)
- **로그인/회원가입**: Firebase 기반(구글/애플/이메일) + 카카오(Cloud Function 스캐폴드).
  - 신규 `src/firebase.js`, `src/store/useAuth.js`, `src/components/LoginView.jsx`,
    `functions/`(index.js·package.json·README), `.env.example`
- **홈 대시보드**: 인사·요약·메뉴 카드 화면을 기본 진입 탭으로. (`src/components/HomeView.jsx`)
- `firebase` 의존성 설치, `.env`는 gitignore 처리
- 빌드 통과, GitHub `main` 푸시

> 참고: 실제 소셜 로그인을 쓰려면 사용자가 Firebase 프로젝트 생성 + `.env` 입력 +
> (카카오) Cloud Function 배포가 필요. 자세한 내용은 `.env.example`, `functions/README.md`.
